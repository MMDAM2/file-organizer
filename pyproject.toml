#:schema https://json.schemastore.org/pyproject.json
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hidden"
version = "0.2.0"
description = "idk"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/"]

# cSpell: words pylint
[tool.pylint.messages_control]
disable = ["fixme", "unused-argument"]
